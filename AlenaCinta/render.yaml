services:
  - type: web
    name: alena-website
    env: node
    # Remove rootDirectory - let Render auto-detect or set in dashboard
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
